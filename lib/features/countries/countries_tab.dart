import 'package:flutter/material.dart';

class CountriesTab extends StatelessWidget {
  const CountriesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(child: Text('Countries Tab')),
    );
  }
}
